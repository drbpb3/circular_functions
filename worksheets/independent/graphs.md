So far, we have explored circular functions through their representations on a unit circle. Of course, you know only too well that we can draw graphs like $y=\sin x$, $y=\cos x$, and $y=\tan x$. The question is, what is the relationship between these two representations of the functions. You can watch a fancy animation like this

<video>src="/graphs_images/singraph.mp4"</video>

and it looks very convincing, but it's easy to miss the details. So start by working on this video here, which builds up the relationship between the unit circle and the graph $y=\sin x$ very gradually to make it crystal clear. 

The relationship between the unit circle and the graph $y=\cos x$ is slightly harder to visualize, but this video builds it up in exactly the same way. 

<video>src="/graphs_images/cosgraph.mp4"</video>

Tan is even harder, because now we are mapping a gradient to a point on the $y$ axis. However, try this:

<video>src="/graphs_images/tangraph.mp4"</video>
